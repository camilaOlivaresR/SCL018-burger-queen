import React from "react";
//import imagen

export const NavBar = () => {
    return(
        <nav className="navegation">
            <button>Pedidos en Curso</button>
            <button>Salir</button>
        </nav>
    )
}